[project]
name = "backend"
version = "0.1.0"
description = "Week 3 Backend Service"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-dotenv",
    "pydantic",
    "fastmcp",
    "google-genai",
]

[tool.uv]
dev-dependencies = []
python-preference = "only-system"